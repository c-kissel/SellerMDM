base_path: /sellermdm/api
env_path: ".env"
app_port: ":17100"

allowed:
  hosts:
    - "http://localhost:8080"
    - "http://localhost:8300"

postgres:
  use: true
  username: "postgres"
  host: "smdmsql"
  port: "10100"
  db_name: "smdm"
  ssl_mode: "disable"
